# Keep the model outside the payment boundary

Gemini extracts explicit purchase constraints into a fixed schema, but deterministic policy alone decides whether Razorpay order creation is allowed. This rejects a more autonomous agent in exchange for auditable money controls, predictable adversarial tests, and graceful operation when the model is unavailable.
