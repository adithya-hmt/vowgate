{
  "name": "vowgate",
  "version": "1.0.0",
  "private": true,
  "description": "The mandate firewall for agentic commerce", 
  "author": "Adithya S",
  "type": "module",
  "scripts": {
    "start": "node local.js",
    "test": "node --test",
    "check": "node --check local.js && node --check lib/http.js && node --check lib/mandate-state.js && node --check lib/order.js && node --check public/app.js && node --test"
  },
  "engines": {
    "node": "24.x"
  }
}
